case chan const default defer go go if import interface map package range select default defer else for func go go if import interface map range select default defer else for func go go if import interface map package range struct switch for func go go if import interface map package range select default defer else for func go go if import range struct switch for func go go if import interface map package range select default defer else for func go go if import interface map package range struct switch var